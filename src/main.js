import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
import 'bootstrap/dist/css/bootstrap.min.css'
import 'bootstrap' // (opcional, se quiser os JS components como modal, dropdown etc.)

createApp(App).mount('#app')
